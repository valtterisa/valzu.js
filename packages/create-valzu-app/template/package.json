{
  "name": "my-valzu-app",
  "version": "1.0.0",
  "scripts": {
    "build:client": "esbuild src/client.tsx --bundle --minify --outfile=dist/client.js --format=esm",
    "build": "npm run build:client",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "valzu-core": "^1.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.15.0"
  }
}
