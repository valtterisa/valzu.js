# Changesets

Add a changeset when you change a publishable package:

- `pnpm changeset` — add a new changeset (choose packages and bump type)
- `pnpm version` — apply changesets, bump versions, update changelogs (run `pnpm install` after)
- `pnpm publish:packages` — build and publish all version-bumped packages to npm
